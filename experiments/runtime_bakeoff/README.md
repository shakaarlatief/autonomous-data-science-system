# V1 Runtime Bakeoff

Executable evidence for Specification 005 will live here. The experiment is intentionally framework-neutral at the harness boundary. Candidate runtime packages must remain behind adapter-specific modules and must not define ADS project/domain semantics.
