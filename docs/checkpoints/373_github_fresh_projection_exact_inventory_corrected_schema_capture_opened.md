# Checkpoint 373: GitHub Fresh Projection Exact Inventory Corrected, Schema Capture Opened

**Date:** 2026-09-08
**Status:** PASS / FRESH 89-ACTION INVENTORY EXACT / CHECKPOINT 372 NAME RECONSTRUCTION CORRECTED / SCHEMA BATCH 1 NEXT
**Checkpoint class:** RESEARCH EVIDENCE CORRECTION + PREIMPLEMENTATION QUALIFICATION BOUNDARY
**Project stage:** Research 123 GitHub connector capability parity and Codexless Runtime Bridge architecture
**Scope:** Preserve the exact fresh 89-action GitHub projection, correct the single Checkpoint 372 exact-name reconstruction defect, and open projected-order native schema capture without beginning implementation.
**Authority:** Validation 130 owns the fresh projection/correction evidence; `docs/research/github_connector_89_action_inventory.json` owns the corrected machine-readable action inventory; Checkpoint 372 remains historical architecture evidence.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-20`
**Conversation title:** `20 - GitHub Capability Parity and Codexless Runtime Bridge`
**Primary collaborator:** ChatGPT

## 1. Fresh native projection remains 89 actions

The user opened the required fresh GitHub-only discovery conversation and performed no GitHub action invocation. The host projected exactly 89 GitHub actions, matching the two count-level qualifications preserved in Validation 128.

The fresh exact ordered inventory is now preserved directly rather than reconstructed from prior prose.

## 2. One Checkpoint 372 name reconstruction was wrong

Comparison found:

```text
fresh projection only        GitHub.download_user_content
Checkpoint 372 inventory only GitHub.add_issue_comment
```

`GitHub.add_comment_to_issue` is the projected issue-comment creation action. `GitHub.add_issue_comment` is not projected.

Because the original Validation 128 did not publish all exact names, this is classified as a reconstruction defect at Checkpoint 372, not as evidence that the connector changed between the qualifications.

Checkpoint 372 is not silently rewritten. Validation 130 and this checkpoint explicitly supersede only its exact-name reconstruction.

## 3. Corrected inventory is mechanically guarded

The machine inventory is now schema version 2 and preserves the exact fresh projected order. Its family accounting is:

```text
issues                      17
pull requests / reviews     32
repository / Git            29
content download             1
Actions / CI                 9
repository permission        1
TOTAL                        89
```

The inventory validator now checks the full projected action order and the `download_user_content` / `add_issue_comment` correction in addition to count and uniqueness.

## 4. Architecture remains intact

The correction does not alter the selected Research 123 authentication/API architecture. Exact remote Runtime Bridge parity remains `0 / 89`, and implementation remains blocked on full native schema capture.

```text
AUTH_DIRECTION            GITHUB_APP_USER_TOKEN_DEVICE_FLOW
REPOSITORY_SCOPE          GITHUB_APP_INSTALLATION
API_TRANSPORT             INTERNAL_REST_GRAPHQL_CLIENT
IMPLEMENTATION            NOT_STARTED
```

`GitHub.download_user_content` remains deliberately transport-unclassified until its native schema is captured. No guessed endpoint or behavior is accepted as parity evidence.

## 5. Exact continuation

Remain in the same fresh GitHub-only conversation and capture schemas in projected-order batches. Batch 1 is actions 1 through 15 from the fresh projection.

Do not invoke any GitHub action during schema capture.

```text
CHECKPOINT373=FRESH_GITHUB_EXACT_INVENTORY_CORRECTED
FRESH_ACTION_COUNT=89
ACTIONS_INVOKED=0
CHECKPOINT372_EXACT_NAME_ERROR=ONE_ACTION
CORRECTION=download_user_content_REPLACES_add_issue_comment
INTERPRETATION=RECONSTRUCTION_DEFECT_NOT_CONNECTOR_DRIFT
SCHEMA_CAPTURE=ACTIVE
IMPLEMENTATION=NOT_STARTED
RESEARCH123=ACTIVE
RESEARCH113=PAUSED_NOT_CLOSED
SOURCE_VAULT=PAUSED
NEXT=GITHUB_SCHEMA_CAPTURE_BATCH_1
```
