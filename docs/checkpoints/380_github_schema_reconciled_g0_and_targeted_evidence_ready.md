# Checkpoint 380: GitHub Schema Reconciled, G0 and Targeted Evidence Ready

**Date:** 2026-09-08
**Status:** PASS / HOST-VISIBLE 89-ACTION CAPTURE RECONCILED / EXACT WIRE PARITY INCOMPLETE / G0 READY
**Checkpoint class:** RESEARCH RECONCILIATION + PREIMPLEMENTATION BOUNDARY
**Project stage:** Research 123 GitHub connector capability parity and Codexless Runtime Bridge architecture
**Scope:** Preserve the final 89-action schema reconciliation, localize projection loss, reject unnecessary broad native replay, and open the GitHub App/auth/API G0 kernel plus authoritative platform-contract mapping.
**Authority:** Validation 137 owns the final reconciliation interpretation; `docs/research/github_connector_native_schema_capture.json` owns the machine reconciliation; `docs/research/GITHUB_CONNECTOR_PREIMPLEMENTATION_EVIDENCE_GAPS.md` owns the gap-disposition strategy.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-20`
**Conversation title:** `20 - GitHub Capability Parity and Codexless Runtime Bridge`
**Primary collaborator:** ChatGPT

## 1. The six-batch discovery program is closed

The final GitHub-only reconciliation confirms exactly 89 projected actions, exactly 89 captured host-visible contracts, zero missing actions and zero extras. No GitHub action was invoked during schema capture or reconciliation.

The final marker is nevertheless:

```text
GITHUB_89_SCHEMA_CAPTURE=INCOMPLETE
```

because every output schema and structured error schema is hidden by the host projection and `create_tree` contains a genericized nested input.

## 2. Incomplete does not mean restart the 89-action capture

The project now distinguishes practical capability mapping from exact native wrapper wire cloning:

```text
inventory/action names                    complete
host-visible request mapping              complete
exact native wrapper output/error wire    incomplete
```

Running every native action merely to sample wrapper outputs is not justified. It would be expensive, mutation-heavy and still would not establish all optional/error variants.

## 3. G0 is opened

The independent GitHub authority/API kernel can proceed because the remaining projection gaps are action-specific and G0 does not freeze any `github.*` action contract.

```text
G0
    GitHub App identity/configuration
    device-flow user authorization
    protected access/refresh token lifecycle
    installation-derived repository authority
    internal REST/GraphQL transport
    bounded GitHub API routing
    semantic transport-error normalization
```

Action-specific parity publication remains gated by relevant evidence.

## 4. Evidence program changes shape

The next evidence phase is no longer another all-action connector qualification. Use authoritative GitHub platform documentation to fill GitHub-native request/result/permission/status behavior, beginning with gaps such as `create_tree` and cross-field semantics. Use targeted native connector actions only if a wrapper-only behavior materially affects capability or ChatGPT usability and survives the platform mapping.

The hidden `any` outputs should lead to explicit stable Runtime Bridge result contracts, not undocumented claims of native wrapper wire identity.

## 5. Current boundary

```text
CHECKPOINT380=GITHUB_SCHEMA_FINAL_RECONCILIATION_PRESERVED
HOST_VISIBLE_SCHEMA_CAPTURE=89_OF_89_COMPLETE
EXACT_NATIVE_WRAPPER_WIRE_CONTRACT=INCOMPLETE
PRACTICAL_ACTION_MAPPING=READY
G0=READY_TO_BEGIN
BROAD_NATIVE_REPLAY=NOT_JUSTIFIED
ACTION_PUBLICATION=PER_ACTION_EVIDENCE_GATED
RESEARCH123=ACTIVE
RESEARCH113=PAUSED_NOT_CLOSED
SOURCE_VAULT=PAUSED
NEXT=G0_PLUS_AUTHORITATIVE_GITHUB_API_CONTRACT_MAPPING
```
