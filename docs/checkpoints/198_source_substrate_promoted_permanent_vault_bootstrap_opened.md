# Checkpoint 198: Source Substrate Promoted and Permanent Vault Bootstrap Opened

**Date:** 2026-08-25  
**Status:** Promoted source substrate / permanent deployment boundary opened  
**Checkpoint class:** Stage transition / operationalization boundary  
**Project stage:** V1 methodological knowledge-universe construction, permanent source-corpus bootstrap  
**Scope:** Record PR #74 promotion, freeze the exact promoted integration SHA, and open the user-controlled permanent source-vault deployment before Course 2  
**Authority:** Current continuation boundary. Checkpoint 196 remains authoritative for Specification 023 acceptance evidence and Checkpoint 197 for promotion reconciliation.  
**Design session:** 06  
**ChatGPT project:** Autonomous Data Science System  
**Session title:** 06 - Methodological Knowledge Universe Construction

## 1. PR #74 promoted successfully

The fully reconciled promotion candidate:

```text
a1200c034bc00fe11cb0516138127555cf3148b6
```

passed every applicable pull-request workflow, including:

```text
Checkpoint metadata
Current routing consistency
V1 source universe substrate
V1 reasoning context value
V1 methodological horizon
V1 first MethodologicalHorizon builder
V1 retrieval fusion comparator
V1 selective methodological context
V1 autonomous live experiment launcher CI
V1 blocking calibration diagnostic
V1 disposition semantics diagnostic
```

The source-substrate workflow passed its Ubuntu and Windows jobs, including clean migration, the current Python regression suite, and the no-source-binary guard.

PR #74 was then merged using the validated exact head.

Exact promoted V1 integration commit:

```text
v1-frontend-spike
8215718db3e44f000cc6ed53d6a051522d429dbd
```

The merge commit is GitHub-verified.

---

## 2. Accepted source-substrate state

Specification 023 remains classified:

```text
SOURCE_SUBSTRATE_ACCEPTED
```

The accepted source architecture is governed by:

```text
Foundation 022
Research 034
Specification 023
Checkpoint 196
```

Foundation 021 remains the professional product/interface foundation. Checkpoint 197 preserves the numbering-only correction that moved the source foundation from its collided draft number 021 to canonical Foundation 022.

---

## 3. New active branch and PR

The next bounded stage begins from the exact promoted commit:

```text
branch  v1-source-vault-bootstrap
base    8215718db3e44f000cc6ed53d6a051522d429dbd
PR      #75
```

PR #75 remains draft because the permanent user-controlled deployment has not yet been executed.

The first operational runbook is:

```text
docs/source_universe/PERMANENT_VAULT_BOOTSTRAP.md
```

---

## 4. Why a separate deployment stage is required

The first 20-file VU Amsterdam Machine Learning exercise proved the architecture and implementation using exact uploaded bytes and disposable development storage.

It did not create the permanent user-controlled source corpus.

The remaining distinction is:

```text
accepted Source Universe implementation
    !=
permanent Source Registry + SourceArtifactStore deployed on user-controlled durable storage
```

The second condition must be demonstrated before the project admits more educational source batches.

---

## 5. Physical locations required from the user environment

The deployment must resolve five local/private locations without committing them to Git:

```text
ORIGINAL_SOURCE_ROOT
    existing original VU Machine Learning folder

SOURCE_REGISTRY_DATABASE
    permanent SQLite Source Registry path

SOURCE_VAULT_ROOT
    permanent private content-addressed source store

INDEPENDENT_BACKUP_ROOT
    separate recoverable backup destination

CLEAN_RESTORE_ROOT
    temporary clean target for disaster-recovery proof
```

The original course folder remains an intake source and is not modified by ADS.

The source vault and backup must remain outside the public repository.

---

## 6. Frozen next operational sequence

Before Course 2:

```text
1. choose and verify the five user-controlled locations
2. migrate a clean permanent Source Registry to Alembic head
3. compare the original Machine Learning folder against the prospectively frozen manifest/fingerprints
4. inspect every MATCH / DIFFERENT_ARTIFACT / MISSING_LOCAL_SOURCE / ADDITIONAL_LOCAL_SOURCE result
5. prospectively adjust the reviewed manifest only if the original folder requires it
6. ingest the intended corpus
7. run a clean integrity audit
8. create an independent verified backup
9. restore into a clean target
10. run a full restored integrity audit
11. preserve only safe deployment evidence in Git
12. classify the permanent bootstrap
13. only then admit Course 2
```

No mismatch is to be silently normalized.

---

## 7. Human involvement boundary

The repository and accepted CLI can perform the source operations, but this stage requires real paths and storage locations on the user's computer or controlled storage.

The project must not invent or assume those paths.

Therefore the next user-facing action is limited and concrete: identify the original Machine Learning folder and choose where the permanent registry/vault and independent backup should live. Once those locations are known, the exact commands can be generated and executed locally.

---

## 8. Promotion audit

### Foundation

No new foundation required. Foundation 022 remains current.

### Specification

No new specification required yet. This is operationalization of accepted Specification 023.

### Decision

No new storage-provider decision is justified. Local-first, provider-neutral deployment remains sufficient.

### Current routing

Reconciled to Checkpoint 198, branch `v1-source-vault-bootstrap`, PR #75, and promoted SHA `8215718db3e44f000cc6ed53d6a051522d429dbd`.

### Implementation

The accepted source implementation is already promoted. PR #75 should contain deployment runbook/evidence and only implementation changes that real permanent deployment demonstrates are necessary.

---

## 9. Continuation point

Next:

> **Resolve the original VU Machine Learning folder path plus durable Source Registry, Source Vault, independent backup, and temporary clean-restore locations on the user's environment; then execute the permanent bootstrap exactly through compare, ingest, audit, backup, restore, and restored audit before admitting Course 2.**
