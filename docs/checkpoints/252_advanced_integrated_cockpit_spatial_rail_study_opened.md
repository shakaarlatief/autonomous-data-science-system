# Checkpoint 252: Advanced Integrated Cockpit Spatial Rail Study Opened

**Date:** 2026-08-28  
**Status:** Current human-review checkpoint  
**Checkpoint class:** CONTINUITY / PRODUCT_DESIGN / WHOLE_PRODUCT_INTERACTION  
**Project stage:** V1 next-generation Project Cockpit advanced whole-product design exploration on the source-faithful integrated substrate  
**Scope:** Records the transition from fidelity reconstruction into advanced whole-Cockpit design iteration for the right-side spatial tool surface. Opens human comparison of three direct-manipulation rail candidates without selecting any candidate.  
**Authority:** Current routing boundary for the rail study. Existing accepted Phase-C mechanisms and Specification 008 retain their established authority. The A/B/C rail candidates remain provisional until explicit human selection.  
**Interaction environment:** ChatGPT  
**Project / workspace:** Autonomous Data Science System  
**Interaction session:** `chatgpt-09`  
**Conversation title:** `09 - Project Cockpit Design Exploration`  
**Primary collaborator:** ChatGPT

## 1. Boundary transition

Checkpoint 251 opened source-faithful holistic reintegration after implementation provenance was recovered.

That reintegration is now sufficiently complete and protected to serve as the **whole-product design evaluation substrate**:

```text
accepted lower-level mechanisms
    remain source-faithful and regression protected

whole-product shell choices
    remain explicitly provisional

new design exploration
    occurs on the integrated Cockpit
    rather than returning to disconnected miniature fixtures
```

This is the workflow requested during human review: make the Cockpit and Conversation increasingly real, then evaluate new shell/layout ideas directly in the complete environment.

## 2. Product Surface Study A context

Before this checkpoint, Product Surface Study A had already begun replacing reintegration scaffolding with a more product-like shell:

```text
continuous viewport-owned project grid
compact project identity HUD
right-side spatial tool rail
invoked Jump/search surface
normal Conversation typography
more compact full Conversation composer
```

These remain design candidates rather than accepted visual baseline decisions.

## 3. Human rail prompt

The current flat right-side rail prompted a stronger interaction hypothesis:

```text
make the rail feel spatial / 2.5D or 3D
let the user drag or pull it into the Cockpit
use that movement to reveal more capability or layers
let the user put it away through direct manipulation
avoid relying on the small fold-arrow button as the primary mechanism
```

Research 092 preserves the detailed study rationale and implementation evidence.

## 4. Open candidates

### A · Extruded Blade

```text
compact edge blade
-> pull left
-> progressive widening and labelled controls
-> docked / partial / open states
-> push right to stow
```

### B · Layered Deck

```text
stacked edge surface
-> pull left
-> Navigation / Work / System fan into distinct spatial planes
-> push right to restack
```

### C · Dock and Float

```text
edge-docked rail
-> pull beyond detach threshold
-> move as a bounded floating Cockpit object
-> return to right-edge snap zone
-> release to redock
```

No candidate is selected.

## 5. Live review routes

```text
A · Blade
http://localhost:4173/design-lab/cockpit-reintegration.html?focus=map&work=v&rail=blade

B · Layers
http://localhost:4173/design-lab/cockpit-reintegration.html?focus=map&work=v&rail=deck

C · Float
http://localhost:4173/design-lab/cockpit-reintegration.html?focus=map&work=v&rail=float
```

Each route also exposes a lab-only A/B/C switcher so the variants can be compared in the same integrated project context.

## 6. Direct-manipulation rule

Inside these study variants:

```text
drag / pull
    is the primary interaction under evaluation

legacy fold arrow
    remains part of prior architecture evidence
    but is hidden in the candidate studies

real Cockpit controls
    are moved and regrouped
    not replaced by fake study buttons
```

The study therefore tests actual shell behavior rather than static appearance mockups.

## 7. Semantic and focus invariants

Rail manipulation must not mutate:

```text
WorkUnit category
disposition
operational status
attention
selection
relation meaning
relation directionality
project camera meaning
Conversation ownership
X5 / Z7 semantic state
```

Full-focus Conversation and specialist deep focus retain stage ownership. The rail study disappears in those states instead of competing with them.

## 8. Technical validation

Implementation target before preservation:

```text
30f92a55537a9b0a2ec14695ed2982ded4ec9c0e
```

Final holistic browser workflow:

```text
run       33197594115
job       98938593583
result    SUCCESS
coverage  56 / 56 browser tests passing
```

This run includes:

```text
all existing source-faithful Cockpit regression checks
Product Surface Study A checks
Conversation readability / compact composer checks
A Extruded Blade direct-manipulation checks
B Layered Deck separation checks
C Dock and Float detach/move/redock checks
full-focus ownership checks
```

The fidelity workflow path filters were also expanded so future changes to Product Surface Study A and spatial-rail study files automatically trigger the holistic gate.

## 9. Preservation status

```text
accepted Phase-C mechanisms              unchanged
Specification 008                        unchanged
Product Surface Study A                  provisional
A · Extruded Blade                       candidate / unselected
B · Layered Deck                         candidate / unselected
C · Dock and Float                       candidate / unselected
production /cockpit                      untouched
scientific experiment status             unchanged / INCOMPLETE
```

## 10. Current human-review gate

The next actor is the human reviewer.

Review should compare the candidates for:

```text
resting calmness
discoverability of the grip
quality of direct manipulation
functional value of depth
visual restraint versus gimmick risk
occlusion of project work
clarity of grouping
recovery / stowing behavior
potential to support future Cockpit capability
```

A hybrid or refinement may be explored only after concrete review evidence.

## 11. Exact next step

```text
open A, B and C in the complete integrated Cockpit
physically drag each candidate
record what feels good or wrong
preserve those observations
then refine / reject / combine on evidence
```

No rail design is promoted at this checkpoint.