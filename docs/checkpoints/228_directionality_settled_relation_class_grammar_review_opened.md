# Checkpoint 228: Directionality Settled, Relation-Class Grammar Review Opened

**Date:** 2026-08-27  
**Status:** Current product-design checkpoint  
**Checkpoint class:** CONTINUITY / PRODUCT_DESIGN  
**Project stage:** V1 next-generation Project Cockpit browser-rendered design exploration  
**Scope:** Records positive human verification of the simplified arrow-directionality grammar and opens the next bounded semantic relation-class visual-grammar experiment.  
**Authority:** Current Phase-C routing boundary. Foundation 024 remains the durable connector-treatment / hover / directionality architecture. Final relation taxonomy and class encoding remain unfrozen.

## 1. Directionality gate closed

Human review of the simplified D0-D3 browser result:

```text
Yes, perfect. We can proceed.
```

Directionality is therefore sufficiently settled for this design phase:

```text
D0  Undirected      no arrow
D1  Forward         arrow at B
D2  Reverse         arrow at A
D3  Bidirectional   same arrow at both endpoints
```

The restrained edge-connected arrow geometry from Research 056 is retained.

## 2. Connector architecture carried forward

```text
connector treatment
    Clean
    Micro dots
    Frame sockets
    Direction arrows

hover / focus
    separate mechanism
    reveal / intensify / persistent behavior

semantic direction
    system-owned
    not altered by appearance preference
```

Unnecessary stacked terminal combinations remain rejected by default.

## 3. Next bounded slice

New design question:

> How should different semantic relationship classes remain distinguishable on the Cockpit map while staying professional, learnable and scalable?

Research:

```text
docs/research/057_semantic_relation_class_visual_grammar_experiment.md
```

Browser route:

```text
frontend/design-lab/relation-class-grammar.html
frontend/design-lab/relation-class-grammar.css
frontend/design-lab/relation-class-grammar.js
```

Local URL:

```text
http://localhost:5173/design-lab/relation-class-grammar.html
```

Exact browser implementation target:

```text
9ac3a0a0f51c024d0deec2fe54f11735f4cdd0fb
```

## 4. Representative semantic fixtures

```text
R0  Chronology / Sequence
R1  Dependency / Prerequisite
R2  Causal / Influence
R3  Evidence / Support
R4  Lineage / Derivation
```

These are visual-test fixtures only. They do not freeze the ADS relation ontology.

Runtime-flow relationships remain deferred from this first semantic-class comparison.

## 5. Encoding families

```text
E0  Neutral Control
E1  Semantic Hue
E2  Stroke Rhythm
E3  Explicit Tag
E4  Hue + Stroke
E5  Hue + Tag
E6  Restrained Hybrid
```

All rows hold A -> B direction constant so relation meaning is isolated from directionality.

## 6. Human review gate

The project owner should compare:

```text
semantic distinction
professional appearance
map-scale legibility
competition with node-category color
stroke-pattern clarity
tag density
accessibility redundancy
large-project noise risk
```

The result may be prefer / reject / combine / refine. No artificial single-winner requirement applies.

## 7. Production boundary

No production `/cockpit` files changed.

No final relation taxonomy is promoted.

No final line color, dash, label or glyph vocabulary is promoted.

The source-vault bootstrap remains paused and the Course 2 gate is unchanged.
