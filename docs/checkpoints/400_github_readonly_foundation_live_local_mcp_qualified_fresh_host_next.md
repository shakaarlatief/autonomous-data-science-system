# Checkpoint 400: GitHub Read-Only Foundation Live Local MCP Qualified, Fresh-Host Projection Next

**Date:** 2026-09-09
**Status:** PASS / PREVIEW.28 LIVE / FOUR READ-ONLY GITHUB TOOLS / FRESH-HOST QUALIFICATION NEXT
**Checkpoint class:** INFRASTRUCTURE
**Project stage:** Research 123 GitHub parity plus Codexless extensions
**Scope:** Preserve the first live public `github.*` read-only foundation, authenticated GraphQL identity, installation-derived REST repository scope and the same-conversation stale-projection boundary before fresh-host qualification.
**Authority:** Validation 157 owns detailed release, health, schema and live-action evidence. Research 123 owns the parity classification and continuation boundary.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** chatgpt-21
**Conversation title:** 21 - GitHub Device Flow Recovery and Authorization Diagnosis
**Primary collaborator:** ChatGPT

Private local-runtime head `a18983d2b2199f350d07664cfe91dbf013e2df3c` preserves final immutable release `github-readonly-foundation-v3`. Its sixteen-regression publication succeeded after migrating the historical G0 zero-public-action assertion to the intentional four-action contract. Bounded restart activation succeeded without recovery, and postactivation verification reports version `0.1.1-preview.28-github-readonly-foundation`, 68 tools, one runtime dependency and zero mismatches.

Direct local health reports preview.28 / 68 tools. Tunnel `/healthz` and `/readyz` both return HTTP 200. Protected GitHub authorization survived the restart with `authorized=true`, `storedAuthorization=true`, and non-expired access/refresh lifetimes.

A fresh stateless local MCP session then discovered and successfully invoked:

```text
github.get_profile
github.get_user_login
github.list_installations
github.list_repositories_by_installation
```

Live GitHub evidence proves authenticated login `shakaarlatief`, one App installation with `repositorySelection=all`, twelve repositories accessible through that installation, and presence of `shakaarlatief/autonomous-data-science-system`. The server-owned GraphQL viewer query succeeded. Other repository names and all credential values were intentionally omitted from preservation.

The bundle is intentionally classified as a read-only **foundation**, not four fully closed native-parity rows. `github.list_installations(manageable_only=true)` fails closed with `GITHUB_PARITY_OPTION_NOT_QUALIFIED` because the native connector did not project exact managed-account filter semantics. Exact parity therefore remains 0/89 until host qualification and the remaining native-contract gap are closed.

The active MCP server exposes 68 tools, but this existing `chatgpt-21` connector projection still does not expose any of the four new names. AB-008 therefore requires Plugin refresh plus a fresh disposable ChatGPT conversation for host-visible schema capture and one bounded live read of each new action.

```text
CHECKPOINT400=GITHUB_READONLY_FOUNDATION_LIVE_LOCAL_MCP_QUALIFIED
LIVE_RUNTIME_VERSION=0.1.1-preview.28-github-readonly-foundation
LIVE_MCP_TOOL_COUNT=68
LIVE_GITHUB_FOUNDATION_TOOLS=4
EXACT_PARITY_ROWS_CLOSED=0
GITHUB_USER_AUTHORIZED=true
STORED_AUTHORIZATION=true
PERSONAL_INSTALL_SCOPE=ALL_REPOSITORIES
GRAPHQL_VIEWER_QUERY=PASS
LOCAL_INSTALLATION_ENUMERATION=PASS
LOCAL_REPOSITORY_SCOPE_ENUMERATION=PASS
SAME_CHAT_NEW_TOOL_PROJECTION=STALE
NEXT=REFRESH_PLUGIN_AND_FRESH_CHAT_GITHUB_READONLY_FOUNDATION_QUALIFICATION
```
