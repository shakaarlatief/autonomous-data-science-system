# Checkpoint 402: GitHub Read-Only Foundation Fresh-Host Qualified, G1 Expansion Next

**Date:** 2026-09-09
**Status:** PASS / FRESH-HOST FOUR-ACTION FOUNDATION QUALIFIED / G1 READ-ONLY EXPANSION NEXT
**Checkpoint class:** INFRASTRUCTURE
**Project stage:** Research 123 GitHub parity plus Codexless extensions
**Scope:** Preserve the refreshed fresh-ChatGPT-host projection and live-read PASS for the four already-live Runtime Bridge GitHub foundation actions and advance Research 123 to the remaining G1 identity/account/repository/permission reads.
**Authority:** Validation 159 owns the fresh-host result supplied by the owner. Validation 157 remains exact local MCP schema/release evidence. Research 123 owns parity interpretation and continuation.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** chatgpt-21
**Conversation title:** 21 - GitHub Device Flow Recovery and Authorization Diagnosis
**Primary collaborator:** ChatGPT

The fresh disposable ChatGPT qualification closes the stale-projection gate left by Checkpoint 400 and resumed after the Checkpoint 401 semantic-Git reliability correction. All four exact public foundation actions projected through the refreshed host and all four authorized calls succeeded read-only in the required order.

`github.get_profile` and `github.get_user_login` agree on authenticated login `shakaarlatief`. `github.list_installations(manageable_only=false)` returns one personal User installation with `repositorySelection=all`. `github.list_repositories_by_installation(page_size=20,page_offset=0)` returns 12 of 12 repositories with `hasMore=false` and confirms `shakaarlatief/autonomous-data-science-system` is inside installation-derived scope.

The fresh-host result reports bounded action schemas and no caller-selected token, credential, host, URL, REST endpoint, GraphQL document, HTTP method/header, permission profile or equivalent arbitrary transport authority. No access/refresh token, device code, client secret, Authorization header or credential-store payload was exposed, and no GitHub mutation occurred. Installation ID and unrelated private repository names remain intentionally unpreserved.

The exact Part A host-schema transcript was not copied into the persistent project chat, so the checkpoint does not fabricate it. Validation 157 already owns the exact local MCP serialized contracts, while Validation 159 owns fresh-host presence/boundedness and successful live reads.

The known native `list_installations(manageable_only=true)` semantic gap remains open because only `false` was exercised and the native connector never projected the exact managed-account filter set. Exact native-wrapper wire parity therefore remains conservatively 0/89. This does not diminish the now-qualified four-action Runtime Bridge capability foundation.

The next Research 123 implementation slice remains G1 and should extend read-only identity/account/repository discovery and permission coverage before G2 fetch/search/branch/commit/file reads. Strong next candidates are `github.get_repo`, `github.get_repo_collaborator_permission`, `github.list_installed_accounts`, `github.list_repositories`, `github.list_repositories_by_affiliation`, `github.list_user_org_memberships`, and `github.list_user_orgs`. Their exact native request contracts are already captured; action-specific implementation and qualification must preserve those contracts without inventing hidden native output envelopes.

```text
CHECKPOINT402=GITHUB_READONLY_FOUNDATION_FRESH_HOST_QUALIFIED
GITHUB_READONLY_FOUNDATION_FRESH_HOST=PASS
FRESH_HOST_ACTIONS_PROJECTED=4_OF_4
FRESH_HOST_LIVE_READS=4_OF_4_PASS
AUTHENTICATED_LOGIN=shakaarlatief
PERSONAL_INSTALL_SCOPE=ALL_REPOSITORIES
CANONICAL_ADS_REPOSITORY_IN_SCOPE=true
SECRETS_EXPOSED=false
GITHUB_MUTATION_OCCURRED=false
MANAGEABLE_ONLY_TRUE=NOT_QUALIFIED
EXACT_NATIVE_PARITY_ROWS_CLOSED=0
RESEARCH123=ACTIVE
NEXT=G1_IDENTITY_ACCOUNT_REPOSITORY_PERMISSION_READONLY_EXPANSION
```
