# Checkpoint 310: Fresh Chat Host ResourceLink Not Advertised, Multi-PDF Direct Access Next

**Date:** 2026-09-06
**Status:** FRESH HOST CAPABILITY DISCRIMINATOR RESOLVED / `resourceLink` NOT ADVERTISED / MULTI-NATIVE-PDF QUALIFICATION NEXT
**Checkpoint class:** LOCAL EXECUTION / DIRECT CHATGPT FILE ACCESS
**Project stage:** Research 117 / Research 119 direct local-file access
**Scope:** Preserves the fresh disposable ChatGPT host-capability result, the distinction between the successful MCP App handshake and the request-local diagnostic result-store bug, and the return to deterministic multi-native-PDF `document_file_link` qualification.
**Authority:** Validation 068 is the detailed evidence. Research 119 remains the governing objective correction.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-18`
**Conversation title:** `18 - Astra Architecture Review and Multimodal Handoff Continuation`
**Primary collaborator:** ChatGPT

The fresh disposable qualification conversation successfully exposed the Checkpoint 309 probe tools and mounted the MCP App. Its visible `ui/initialize` snapshot advertised `updateModelContext: {}` but did not include a `resourceLink` modality.

Accepted current-host classification:

```text
updateModelContext                  ADVERTISED
updateModelContext.resourceLink     NOT_ADVERTISED
```

The later result-tool calls returning `not_recorded` do not overturn that snapshot. The probe's default in-memory store is created inside each server registration while HTTP serving creates a fresh `McpServer` per request. A focused private regression confirms that separate registrations have isolated default stores.

Therefore the proposed small-PDF `resourceLink -> ui/update-model-context` qualification is no longer the next experiment for this host.

The next direct-access discriminator is:

```text
large PDF source
    -> deterministic valid native PDF parts
    -> each part below the known clean host-materialization PASS envelope
    -> codex.document_file_link for every part
    -> same ordinary ChatGPT conversation
    -> verify direct source access across the complete ordered page set
```

This remains direct ChatGPT source access because no reasoning model interprets the source before ChatGPT receives the native PDF parts.

A PASS must be scoped to multi-part native source access. It must not be misreported as whole-file materialization of the unchanged large PDF.

The fresh qualification also resolved the same-chat tool-projection question for this publication cycle:

```text
same persistent chat + Plugin refresh
    new probe tools not projected

fresh disposable chat
    new probe tools projected successfully
```

Browser upload remains a later transport fallback, not the current next action.

```text
CHECKPOINT_310 = HOST_RESOURCE_LINK_NOT_ADVERTISED_MULTI_PDF_NEXT
```
