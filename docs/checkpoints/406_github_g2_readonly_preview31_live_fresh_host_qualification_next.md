# Checkpoint 406: GitHub G2 Read-Only Preview.31 Live, Fresh-Host Qualification Next

**Date:** 2026-09-09
**Status:** PASS / G2 LIVE / FRESH-HOST QUALIFICATION NEXT
**Checkpoint class:** INFRASTRUCTURE
**Project stage:** Research 123 GitHub parity plus Codexless extensions
**Scope:** Preserve live activation of the eleven-action G2 repository fetch/search/branch/commit/file/blob/compare slice and move the boundary to fresh-host projection plus live read-only qualification.
**Authority:** Validation 163 owns preview.31 implementation/release/activation evidence. Validation 162 remains authoritative for the closed G1 7/7 fresh-host gate.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** chatgpt-21
**Conversation title:** 21 - GitHub Device Flow Recovery and Authorization Diagnosis
**Primary collaborator:** ChatGPT

Private local-runtime head `b446a3fc2ff480e66acc81ddad56f12bb871feca` preserves corrected immutable release `github-g2-readonly-expansion-v2`. It targets `0.1.1-preview.31-github-g2-readonly`, 86 public tools, 22 GitHub read-only tools, ten release files, sixteen regressions and one exact runtime dependency.

The earlier v1 path is preserved rather than hidden: its first 17-regression manifest exceeded the existing bounded maximum and was rejected; after correction to sixteen, its first publish failed a new public-wire regression. A reconstructed current-source stage localized the defect to an incorrect test expectation for the nullable commit-search sort enum. The MCP schema itself was correct. Because v1 was already immutably prepared, changed bytes correctly produced a prepared conflict and the repaired release moved to v2.

Corrected v2 publication `rm_59bfec18a65b6d64e35b5ad003f7463d` succeeded after the full release regression matrix. Restart `rm_ad739f5850d15cd2b7eb267d6ca25c0b` activated preview.31 without recovery. A transient HTTP 502 occurred only while reading restart status during the deliberate worker replacement window; the same durable operation later returned `succeeded`, and the restart mutation was not replayed. Postactivation release verification is `verified` with `mismatchCount=0`. Protected GitHub authorization remains configured, stored, authorized and non-expired.

The eleven additions are `github.compare_commits`, `github.fetch`, `github.fetch_blob`, `github.fetch_commit`, `github.fetch_file`, `github.search`, `github.search_branches`, `github.search_commits`, `github.search_installed_repositories_streaming`, `github.search_installed_repositories_v2`, and `github.search_repositories`. The new implementation remains read-only and installation-scoped. Hidden native output/enrichment semantics and Enterprise routing are not inferred, so exact native-wrapper parity remains 0/89.

Targeted rediscovery in this already-open `chatgpt-21` host still exposes only the earlier G1 names. This is the established AB-008 same-chat stale-projection class. The next gate is therefore a refreshed disposable ChatGPT conversation for exact G2 host discovery/schema capture and live read-only qualification. No GitHub mutation is needed.

```text
CHECKPOINT406=GITHUB_G2_READONLY_PREVIEW31_LIVE
LIVE_RUNTIME_VERSION=0.1.1-preview.31-github-g2-readonly
LIVE_PUBLIC_TOOL_COUNT=86
LIVE_GITHUB_READONLY_TOOL_COUNT=22
G2_NEW_TOOL_COUNT=11
G2_RELEASE_V2=PASS
G2_POSTACTIVATION_VERIFY=PASS_ZERO_MISMATCH
PROTECTED_AUTHORIZATION_HEALTHY=true
SAME_CHAT_G2_PROJECTION=STALE
SECRETS_EXPOSED=false
GITHUB_MUTATION_OCCURRED=false
EXACT_NATIVE_PARITY_ROWS_CLOSED=0
RESEARCH123=ACTIVE
NEXT=FRESH_CHAT_G2_SCHEMA_AND_LIVE_READ_QUALIFICATION
```
