# Checkpoint 383: GitHub G0 Windows Keyring Qualified, Runtime Integration Next

**Date:** 2026-09-08
**Status:** PASS / WINDOWS PROTECTED STORE QUALIFIED / MAIN RUNTIME INTEGRATION NEXT
**Checkpoint class:** LOCAL HOST QUALIFICATION BOUNDARY
**Project stage:** Research 123 GitHub connector capability parity and Codexless Runtime Bridge architecture
**Scope:** Preserve the successful normal-user-session Windows Credential Manager qualification for the exact G0 keyring adapter and open main Codexless runtime integration.
**Authority:** Validation 140 owns the host round-trip evidence; Checkpoint 382 / Validation 139 continue to own the private G0 candidate bytes and focused regression result.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-20`
**Conversation title:** `20 - GitHub Capability Parity and Codexless Runtime Bridge`
**Primary collaborator:** ChatGPT

## 1. Protected Windows storage is now live-qualified

The exact `@napi-rs/keyring@2.0.0` package was loaded on Windows x64 and exercised through one newly generated synthetic Credential Manager entry under the `Codexless Runtime Bridge` service identity.

```text
set                 true
readMatch           true
delete              true
absentAfterDelete   true
cleanupAttempted    true
```

No GitHub token was involved. The random synthetic secret stayed in memory and was not printed. Cleanup was verified.

## 2. Earlier sandbox failure is localized

The ordinary command sandbox could import the package but its Credential Manager write failed with `ERROR_NO_SUCH_LOGON_SESSION`. The normal user-session probe succeeded without changing the package or weakening the test. The difference is preserved as execution-context evidence.

## 3. Candidate evidence is sufficient for integration

```text
G0 focused tests                         12 / 12 PASS
package import/API                       PASS
OS credential set/read/delete/cleanup   PASS
```

The temporary installed package/cache material used only for qualification was removed afterward.

## 4. Next boundary

The next work is main-runtime integration, not live GitHub authorization. Integrate the existing G0 modules into a qualified Codexless candidate, define exact dependency packaging/loading, add integration regressions, and preserve zero public `github.*` actions until the internal kernel is release-qualified.

```text
CHECKPOINT383=WINDOWS_KEYRING_HOST_ROUNDTRIP_QUALIFIED
PROTECTED_TOKEN_STORE=IMPLEMENTATION_QUALIFIED
LIVE_GITHUB_AUTH=NOT_STARTED
PUBLIC_GITHUB_SURFACE=NOT_STARTED
LIVE_CODEXLESS_G0=NOT_INSTALLED
RESEARCH123=ACTIVE
RESEARCH113=PAUSED_NOT_CLOSED
SOURCE_VAULT=PAUSED
NEXT=INTEGRATE_G0_INTO_MAIN_CODEXLESS_RUNTIME_CANDIDATE
```
