# Checkpoint 342: Detached Runtime Helper Survival Qualified

**Date:** 2026-09-07
**Status:** PASS / DETACHED HELPER SURVIVAL + DURABLE LEDGER QUALIFIED / DIRECT CODEXLESS LAUNCH NEXT
**Checkpoint class:** ARCHITECTURE / LOCAL EXECUTION / LIFECYCLE QUALIFICATION
**Project stage:** Research 122 runtime self-maintenance and device-independent access
**Scope:** Qualifies the file-backed operation ledger, narrow detached launcher contract, and real survival of a delayed detached helper across a failed/timed-out caller wrapper using only an isolated dummy service.
**Authority:** Research 122 governs the architecture; Validation 100 owns the detailed evidence.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-19`
**Conversation title:** `19 - Hybrid PDF Intent Matrix and Isolation Qualification`
**Primary collaborator:** ChatGPT

The private runtime candidate at `22773bb9d39f29f091b06f90f970730e01bd46d9` passes the original 12-test runtime-maintenance suite plus a new 6-test durable-ledger/detached-launch suite.

A real detached helper probe started dummy generation A, launched a delayed external helper, and then the generic `command_exec` wrapper timed out. A later independent invocation nevertheless observed a durable succeeded receipt and healthy generation B running on the same endpoint. This proves helper survival and recoverable terminal state even when the caller wrapper fails.

The timeout also rejects `command_exec` as the permanent launcher. The next implementation is a direct Codexless-owned semantic runtime-maintenance path that records the operation, spawns the helper directly from Codexless, and returns before the destructive delay.

Combined with Checkpoint 341, the normal target is now tunnel-preserving Codexless-only restart.

```text
CHECKPOINT_342=DETACHED_HELPER_SURVIVAL_QUALIFIED
NEXT=PROCESS_IDENTITY_AND_PRODUCTION_SHAPED_ONE_SHOT_SUPERVISOR
```
