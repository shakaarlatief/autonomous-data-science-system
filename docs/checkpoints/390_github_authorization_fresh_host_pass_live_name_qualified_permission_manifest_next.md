# Checkpoint 390: GitHub Authorization Fresh Host Pass, Live Name Qualified, Permission Manifest Next

**Date:** 2026-09-08
**Status:** PASS / AUTH SUPPORT FRESH-HOST QUALIFIED / LIVE PLUGIN RENAME QUALIFIED / GITHUB APP PERMISSION MANIFEST NEXT
**Checkpoint class:** FRESH-HOST SUPPORT-SURFACE + LIVE DISPLAY-NAME QUALIFICATION
**Project stage:** Research 123 GitHub connector capability parity and Codexless Runtime Bridge architecture
**Scope:** Preserve successful fresh-host projection and metadata execution for the preview.25 flat authorization schema, plus direct UI evidence that the live Plugin display name is now `Codexless Runtime Bridge`.
**Authority:** Validation 147 owns the detailed host/schema/metadata/display-name evidence. Validation 146 owns the live preview.25 release and local wire-schema evidence.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-20`
**Conversation title:** `20 - GitHub Capability Parity and Codexless Runtime Bridge`
**Primary collaborator:** ChatGPT

## 1. Fresh-host preview.25 gate passed

A refreshed fresh disposable ChatGPT conversation projected `codex.github_authorization` as a structured flat bounded schema with visible six-value action enum, bounded optional request/reference fields, and literal `confirmClear=true`. The preview.24 generic-map projection did not recur.

Exactly one `metadata` call succeeded and returned the expected non-secret current state:

```text
configured             false
initialized            false
authorized             false
storedAuthorization    false
authMode               github-app-user-token-device-flow
host                   github.com
restApiVersion         2026-03-10
```

No device flow or credential mutation occurred.

## 2. Live Plugin display-name rename is now qualified

The project owner supplied a current ChatGPT UI screenshot showing the Plugin as:

```text
Codexless Runtime Bridge
```

This closes the display-name qualification explicitly left open at Checkpoint 370. Historical occurrences of `ADS Codexless Local Bridge` remain historical evidence and are not rewritten.

## 3. Authorization is still intentionally unconfigured

```text
GitHub App client ID configured  false
stored GitHub authorization      false
live GitHub authorization        NOT STARTED
public github.* actions           0 / 89
```

## 4. Next boundary

Before creating/configuring the dedicated GitHub App or beginning device flow, derive and freeze the exact minimal GitHub App permission manifest from official endpoint requirements for the 89-action parity target.

This requirement was already part of the frozen Research 123 architecture and is now the immediate implementation gate.

```text
CHECKPOINT390=GITHUB_AUTHORIZATION_FRESH_HOST_PASS
LIVE_PLUGIN_DISPLAY_NAME=Codexless Runtime Bridge
PLUGIN_DISPLAY_RENAME=QUALIFIED
PUBLIC_TOOL_COUNT=64
PUBLIC_GITHUB_ACTIONS=0
LIVE_GITHUB_AUTH=NOT_STARTED
RESEARCH123=ACTIVE
RESEARCH113=PAUSED_NOT_CLOSED
SOURCE_VAULT=PAUSED
NEXT=DERIVE_AND_FREEZE_GITHUB_APP_PERMISSION_MANIFEST
```
