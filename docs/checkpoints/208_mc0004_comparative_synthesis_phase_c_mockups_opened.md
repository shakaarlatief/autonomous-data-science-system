# Checkpoint 208: MC-0004 Comparative Synthesis Frozen, Phase C Mockup Evaluation Opened

**Date:** 2026-08-26  
**Status:** Current collaboration/product-design checkpoint  
**Checkpoint class:** CONTINUITY / PRODUCT_DESIGN / COLLABORATION  
**Project stage:** V1 next-generation Project Cockpit visual and interaction exploration  
**Scope:** Records Claude's completed Phase-B comparative review, freezes the ChatGPT comparative synthesis as Research 038, narrows the visual program to three realistic mockup directions, preserves unresolved product/technology questions, and opens Phase C mockup evaluation without authorizing frontend implementation.  
**Authority:** Current routing and design-evidence boundary. Specification 008 remains the promoted V1 Project Cockpit interaction architecture. Research 038 is design synthesis, not a promoted visual specification.  
**Interaction environment:** ChatGPT  
**Project / workspace:** Autonomous Data Science System  
**Interaction session:** chatgpt-07  
**Conversation title:** 07 - Project Cockpit Design Exploration  
**Primary collaborator:** ChatGPT

## 1. Claude Phase B is valid and complete

Claude's Phase-B comparative review is preserved at:

```text
docs/model_collaboration/threads/MC-0004/messages/002_claude_comparative_review.md
```

with commit:

```text
d94d696214a41d2a3904aa9ce2a42bdab5f2f3ce
```

A direct comparison against the immediately preceding branch head confirms that Claude changed only this one declared secondary collaboration-message surface.

The historical Phase-A classification remains:

```text
BLIND_TO_CANDIDATE
known candidate exposures: none
```

Phase B is correctly classified:

```text
COMPARATIVE_ONLY
```

and does not retroactively alter Phase-A independence.

## 2. Strongest comparative conclusions

The independent and comparative evidence strongly reinforces:

```text
semantic zoom as the highest-confidence first mechanism
category-level work-unit visual grammar
semantic connector type + direction + liveness
motion that is event-driven and calm at rest
full long-form Conversation Workspace
conversation links to structured project state
2.5D before true 3D
multi-axis project/runtime/importance state treatment
```

Research 037 contributes especially strong external product evidence, information-density lenses, multi-axis state separation, and appropriate caution about conversation persistence.

Claude contributes especially strong code-level specificity, a falsifiable connector-motion cap hypothesis, decision-ready recommendation discipline, and a valid scope-creep warning about prototyping too many technology candidates.

## 3. Genuine unresolved disagreement remains explicit

Command architecture at scale remains unresolved.

Current Jump/search has real human-review evidence on the representative small fixture.

A Linear-style contextual command surface is a plausible scale response but has not yet earned replacement.

The resolution gate is a medium/large project test, not model preference.

Other deliberately unresolved questions include:

```text
conversation persistence/threading model
exact visual identity
exact connector styling
exact semantic-zoom thresholds
lens names/count
final stage taxonomy/layout
auto-layout approach
final graph/canvas library
motion library
production 2.5D use
full 3D
```

## 4. Research 038 freezes the comparative synthesis

The synthesis artifact is:

```text
docs/research/038_mc0004_comparative_cockpit_design_synthesis_and_mockup_direction_set.md
```

It narrows the first realistic mockup round to three deliberately distinguishable directions:

```text
M1  Living Precision Canvas          preferred anchor
M2  Spatial Control Room             strongest alternative
M3  Depth-Aware Analytical Workbench bounded high-upside alternative
```

This ranking is a mockup recommendation, not a promoted visual specification.

## 5. Phase C is now open

The next bounded task is realistic visual comparison, not frontend implementation.

All three directions must use the same project scenario and must show more than a resting attractive map.

Required first-round states include:

```text
resting project overview
active investigation
hard blocker
affected downstream relations
completed versus unresolved work
selected work unit
semantic zoom at multiple scales
full long Conversation Workspace
conversation + analytical work coexistence
Execution or Review lens state
light appearance
dark appearance
```

The representative scenario remains Customer Churn Prediction with prediction-moment uncertainty, production missingness investigation, chronological validation, completed baseline, deferred Random Forest benchmark, downstream evaluation, one live run, one waiting-for-human state, and a realistic long transcript.

## 6. Technology scope is deliberately narrowed

Do not begin a broad technology bakeoff during first-round mockups.

The current order is:

```text
visual/product direction first
    ->
then bounded technical uncertainty spikes
```

If later earned, first technical comparators are limited to:

```text
current DOM/CSS/SVG control baseline
React Flow for semantic topology/zoom/grouping comparator
Motion for React for focus/conversation transition comparator if needed
```

PixiJS, Sigma.js, React Three Fiber and full 3D remain out of the next bounded step unless later evidence specifically creates a need.

## 7. Source-vault pause remains unchanged

The permanent source-vault bootstrap remains:

```text
PAUSED
not cancelled
not rejected
not superseded
accepted Source Universe architecture/runbook unchanged
Course 2 recovery-integrity gate unchanged
```

No permanent source-vault operation is performed by this checkpoint.

## 8. No frontend implementation is authorized

This checkpoint authorizes:

```text
visual mockups
interaction compositions
bounded design comparison
human product review
```

It does not authorize:

```text
production React/CSS changes
new graph-library adoption
new motion-library adoption
3D engine introduction
final design-system freeze
implementation specification
```

## 9. Exact continuation

```text
1. use v1-cockpit-design-exploration and Checkpoint 208 as the current route
2. preserve Claude Phase A and Phase B as separate evidence classes
3. use Research 038 as the current comparative synthesis
4. create realistic M1/M2/M3 mockups from the same project scenario
5. explicitly include semantic zoom, semantic relations and the Conversation Workspace
6. compare calm resting state against active/blocking/runtime states
7. include light and dark treatments
8. obtain human product review
9. narrow to one or two preferred directions
10. then create only the bounded technical spikes needed to resolve implementation uncertainty
11. pressure-test medium/large projects and accessibility before implementation architecture is frozen
12. keep the permanent source-vault bootstrap paused until the project owner chooses to resume it
```
