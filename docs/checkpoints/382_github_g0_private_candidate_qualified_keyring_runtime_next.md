# Checkpoint 382: GitHub G0 Private Candidate Qualified, Keyring Runtime Next

**Date:** 2026-09-08
**Status:** PASS / G0 INTERNAL CANDIDATE QUALIFIED / LIVE AUTH + PUBLIC ACTIONS STILL UNCHANGED
**Checkpoint class:** PRIVATE IMPLEMENTATION + REGRESSION QUALIFICATION BOUNDARY
**Project stage:** Research 123 GitHub connector capability parity and Codexless Runtime Bridge architecture
**Scope:** Preserve the first implementation-grade internal GitHub authority/auth/token/REST-GraphQL kernel and its private integrity result before any live credential, OS keyring, runtime publication or `github.*` action surface is introduced.
**Authority:** Validation 139 owns the implementation/test result; the private local-runtime repository at `3c5f3688ec578c0817953890dc69ecb7ce679153` owns the candidate bytes.
**Interaction environment:** ChatGPT
**Project / workspace:** Autonomous Data Science System
**Interaction session:** `chatgpt-20`
**Conversation title:** `20 - GitHub Capability Parity and Codexless Runtime Bridge`
**Primary collaborator:** ChatGPT

## 1. G0 exists as code

The private candidate now implements protected token-store abstraction, device flow, single-flight refresh, fixed github.com REST transport, explicit GraphQL operation registry, semantic errors and installation-derived repository authority.

No live GitHub credential, GitHub action or installed Codexless byte was touched.

## 2. Focused candidate result

```text
npm run check = PASS
npm test      = 12 / 12 PASS
real GitHub requests = 0
real OS credential writes = 0
```

The private runtime commit/push is exact at:

```text
3c5f3688ec578c0817953890dc69ecb7ce679153
RUNTIME_PRIVATE_BOOTSTRAP_SAFETY=PASS
```

## 3. Conservative private secret gate remained authoritative

The first private push failed because normal source-code assignments matched the broad secret-like content scanner. Diagnosis proved this was source syntax/synthetic fixture text, not credential leakage.

The correct response was to change the candidate representation and fixtures. The integrity validator was not weakened. The final scanner match count is zero and the ordinary semantic push passed.

## 4. Next gate

The next bounded work is the concrete Windows protected-store qualification. Install or stage the exact candidate keyring dependency without committing generated/binary package material, verify import/API compatibility on this machine, perform one synthetic credential set/get/delete round trip through the OS credential store under the fixed candidate service/account identity, and prove cleanup. No GitHub access token is needed or allowed for that gate.

Only after the keyring adapter itself is qualified should G0 be integrated into the main Codexless runtime candidate. Public `github.*` actions remain later work.

```text
CHECKPOINT382=G0_PRIVATE_CANDIDATE_QUALIFIED
LOCAL_RUNTIME_HEAD=3c5f3688ec578c0817953890dc69ecb7ce679153
FOCUSED_G0_TESTS=12_OF_12_PASS
PRIVATE_INTEGRITY=PASS
LIVE_GITHUB_AUTH=NOT_STARTED
PUBLIC_GITHUB_SURFACE=NOT_STARTED
RESEARCH123=ACTIVE
RESEARCH113=PAUSED_NOT_CLOSED
SOURCE_VAULT=PAUSED
NEXT=WINDOWS_KEYRING_IMPORT_AND_SYNTHETIC_CREDENTIAL_ROUNDTRIP
```
