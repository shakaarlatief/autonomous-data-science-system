# MC-0004: Next-Generation Project Cockpit Design Exploration

**Thread:** MC-0004  
**Status:** ACTIVE / PHASE C BROWSER DESIGN EVALUATION  
**Review mode:** `INDEPENDENT_THEN_COMPARATIVE`  
**Task owner:** ChatGPT  
**Target-state write owner:** ChatGPT  
**Claude role:** independent reviewer / counter-designer / researcher  
**Human project owner:** final arbiter of product-intent choices  
**Opened:** 2026-08-26

## Collaboration history

```text
Phase A
    Claude independent proposal
    cd2e12f2c79ee3b2f205457c5940eb2022b4631a
    BLIND_TO_CANDIDATE

Phase B
    Claude comparative review
    d94d696214a41d2a3904aa9ce2a42bdab5f2f3ce
    COMPARATIVE_ONLY

Phase C divergent work-unit ideation
    Claude response
    faf18ed9932d60a24dd80589b0ec0ba71c5940fd
    COMPARATIVE_ONLY / DIVERGENT_IDEATION
```

No Claude action is currently pending.

## Preserved accepted Phase-C results

```text
G4 Adaptive Hybrid world
H4 hover/world response
Reduced in-box resting light
scientific marker category grammar
P7 Neutral Tag + Tone disposition
strong current-process focus lens
user-curated focus membership
E5 Hue + Tag relation class
D0-D3 directionality grammar
```

Runtime:

```text
conditional / episode-scoped
Dot + dynamic ring
or
T7 Soft Shade runtime tag
```

Accepted runtime targets:

```text
T7 motion
    08534f94c2f272f969159087de2797a23e36b330

switchable runtime with T7 integrated
    fb847bd65ff6e5e4203a89ee2d4f74b7187c8359
```

BLOCKED / operational-status result:

```text
BLOCKER  = cause
BLOCKS   = relation
BLOCKED  = affected work cannot proceed
FAIL     = failed current execution attempt

BLOCKED compact ring
    sharper non-circular

FAIL compact ring
    smoother circular
```

Exact accepted target:

```text
88fd3c3cfe7a1eff4664afde06341b7b654c97f4
```

## Attention priority result

Research 071 isolated:

```text
ATTENTION PRIORITY
    among visible work, which work deserves more attention now?
```

The project owner selected:

```text
A3  Signal Bars
```

Human response:

```text
I choose A3. Perfect. Proceed.
```

Current accepted Phase-C visual direction:

```text
HIGH attention
    three ascending micro-bars
    near the upper-right frame
```

Exact reviewed browser target:

```text
767c66f76974d3c0a851de0dfa17c502817a4b12
```

Priority scale, ownership, persistence and relationship to relevance/scheduling remain unfrozen.

## Current Checkpoint 240: persistent work-unit selection

The active bounded concept is:

```text
SELECTION
    which work unit has the user explicitly chosen for inspection or action?
```

Selection remains separate from:

```text
hover
keyboard focus
current-process focus membership
attention priority
deep-focus / specialist workspace entry
```

Browser:

```text
http://localhost:5173/design-lab/work-unit-selection-state.html
```

Exact implementation target:

```text
3bac1fea4ca820c89a7bc4516497a4c33164ec5d
```

Candidates:

```text
SEL0  Neutral Control
SEL1  Outer Keyline
SEL2  Corner Brackets
SEL3  Inner Frame
SEL4  Edge Ticks
SEL5  Selection Plate
SEL6  Soft Contour
SEL7  Double Corner
SEL8  Keyline + Corners
```

Interaction proof:

```text
click node
    -> transfer persistent selection

Enter / Space
    -> select keyboard-focused node

Clear selection
    -> remove selected state

pointer hover
    -> remains transient H4 behavior
```

A3 Signal Bars remain fixed on HIGH-attention nodes so selection and priority can be judged together.

## Preserved expansion idea

```text
compact map work unit
    -> expanded contextual/detail card
    -> full specialist workspace / deep focus
```

The current slice stops before expansion. The expanded-card level remains deferred until persistent selection converges, then should be explored with semantic zoom, C5 Internal Layout Grammar, information-density and work-unit detail/provenance presentation.

## Current gate

```text
human compares SEL1-SEL8 against SEL0
human moves selection between practical-scene nodes
human verifies selection persists after pointer exit
human verifies H4 hover, keyboard focus and A3 priority remain distinct
human inspects BLOCKED / FAIL / RUN coexistence
-> prefer / reject / combine / refine
```

Production `/cockpit` remains untouched. No final selection persistence, multi-selection semantics, inline-expansion behavior, semantic zoom implementation or production design-system freeze is authorized.
