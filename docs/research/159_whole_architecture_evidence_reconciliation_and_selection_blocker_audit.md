# Research 159: Whole-Architecture Evidence Reconciliation and Selection-Blocker Audit

**Date:** 2026-09-14
**Status:** WHOLE-ARCHITECTURE EVIDENCE RECONCILED / LEGACY-FILE SEQUENCE REJECTED / Q3 REAL IDENTITY-TEMPORAL FALSIFICATION NEXT / TARGET ARCHITECTURE NOT SELECTED
**Candidate:** `PKA-CANDIDATE-01`
**Scope:** Reconcile all Candidate 01 evidence accumulated through Research 158 across the ten qualification clusters, distinguish design-only/synthetic/real evidence, and choose the next experiment by architectural uncertainty rather than existing-file order.
**Authority:** Qualification-planning evidence only. Requirements V0.2 remain frozen and no final KA-R/KA-I pass is granted by this audit.
**Machine audit:** `docs/research/project_knowledge_candidate_01/WHOLE_ARCHITECTURE_EVIDENCE_RECONCILIATION_V01.json` at SHA-256 `af3672adf26183982515af3e583adae0f93f4337857c0d96d295b55790df9c4b`

## 1. Why this audit is needed now

The recent routing/current-state work produced strong real evidence, but continuing directly into `CONTINUITY.md`, `KNOWLEDGE_MAP.md`, or another legacy artifact would risk turning architecture research into repository refactoring. Candidate 01 must instead be judged as a whole architecture against Requirements V0.2.

The audit therefore treats existing files only as evidence surfaces. It asks which architectural mechanisms remain weak regardless of where today's implementation stores related information.

## 2. Evidence scale

The machine audit uses the following descriptive evidence scale:

```text
0  DESIGN_ONLY
1  SYNTHETIC
2  REAL_SEEDED
3  REAL_ZERO_SEED / REAL_AUDIT / REAL_ZERO_TARGET_INPUT
```

These are evidence-strength descriptors, **not final requirement-pass states**. The qualification matrix still has zero final qualified passes.

Across all 50 KA-R + 17 KA-I items:

```text
DESIGN_ONLY               17
SYNTHETIC                 18
REAL_SEEDED                7
REAL_ZERO_SEED            11
REAL_AUDIT                 9
REAL_ZERO_TARGET_INPUT     5

items with synthetic-or-better evidence   50 / 67
items with some real-repository evidence  32 / 67
final qualified passes                      0 / 67
```

## 3. Cluster-level reconciliation

### Q1 bootstrap / reconstruction / discovery

State: `MIXED_REAL_SUPPORT__MATERIAL_GAPS`.

Real evidence now supports stable bootstrap pieces, task-shaped orientation, progressive disclosure, inspectability, fresh-chat independence and live route/currentness. Remaining architecture-level uncertainty includes:

```text
real high-recall governing/risk discovery
provider/tool portability of the successor representation
material lifecycle distinctions during real reconstruction
integrated bounded cold-continuation budget
```

### Q2 authority resolution / action-contract fidelity

State: `MIXED_REAL_AND_SYNTHETIC__HIGH_CONSEQUENCE_GAPS`.

A real governing procedure has passed the structured contract path, but missing/conflicting authority and consequence-sensitive ambiguity remain mostly synthetic. Known-risk/reopen-trigger activation is still design-only. Free-form high-consequence semantic conformance remains weaker than deterministic constraint-plan checking.

### Q3 identity / relationship / temporal semantics

State: `WEAK__MOSTLY_SYNTHETIC_OR_DESIGN`.

This is the **highest-value immediate falsification target**. No Q3 item yet has Candidate 01 real-repository implementation evidence. Synthetic probes support relationship semantics and representation-independent identity, while explicit epistemic roles, selective temporal/current-history semantics and supersession remain design-only.

This cluster is also the one most likely to reopen H3/Object-Primary architecture if selective source profiles begin accumulating object/transition machinery comparable to a general object substrate.

### Q4 workstream continuation / concurrency

State: `MIXED__REAL_ROUTE_PAUSE__SYNTHETIC_DAG_CONCURRENCY`.

Real evidence now supports active-route reconstruction, durable workstream identity, pause/return semantics and resumable paused state. Multiple dependencies, interruption recovery and stale concurrent authoritative writes remain synthetic.

### Q5 capture / promotion / consolidation fidelity

State: `MIXED_REAL_SUPPORT__PROMOTION_GAPS`.

Real audits support must-preserve consolidation, provenance, active-surface consolidation and capture/authority separation. Explicit promotion is still synthetic, and persistent understanding plus conversation/model independence need integrated successor evidence.

### Q6 derived views / freshness / multi-axis projection

State: `STRONG_REAL_SUBSYSTEM_SUPPORT`.

All four Q6 items now have real Candidate 01 evidence, including zero-seed routing and derived-view separation. Q6 is no longer a dominant uncertainty, though formal final qualification remains pending integrated deletion/rebuild/freshness confirmation.

### Q7 public-private boundary / degraded mode

State: `SYNTHETIC_ONLY`.

All six Q7 items remain synthetic-only. This is a major selection blocker because public/private delegation and non-leakage are constitutional ADS properties, not optional conveniences. A later real layered-continuity qualification is required.

### Q8 scaling / maintenance economics

State: `STRONG_REAL_SUBSYSTEM_SUPPORT`.

All six Q8 items have real evidence through zero-seed routing, current-state decomposition and bounded current-core work. Broader integrated fan-out and recurring-consolidation economics still need confirmation, but Q8 is no longer the dominant architecture uncertainty.

### Q9 migration / authority switch / self-hosting evolution

State: `DESIGN_DOMINANT__REAL_MIGRATION_ANALYSIS_ONLY`.

This remains a major blocker before selection. The project has performed real migration analysis and shadow source relocation, but staged authority switch, reverse-reference reconciliation, rollback and self-hosted redesign continuity are not yet qualified.

### Q10 qualification methodology / budgets

State: `DESIGN_ONLY__PROGRAM_NOT_YET_EXECUTED_AS_FINAL_GATE`.

The qualification methodology exists, but task-specific budgets and the final multidimensional integrated run have not yet been executed. This remains a governing blocker rather than an architecture-feature defect.

## 4. What this changes about the research method

The next method is **not**:

```text
current_routing
-> CURRENT_STATE
-> CONTINUITY
-> KNOWLEDGE_MAP
-> other current files
```

That would make today's repository structure the hidden specification of the successor architecture.

The next method is:

```text
identify the strongest remaining architecture uncertainty
-> design the smallest falsification test for that uncertainty
-> use real repository evidence where it materially strengthens the test
-> reopen architecture families if the candidate becomes unnatural
```

Existing files may appear in a test only because they contain relevant real evidence, not because they must survive.

## 5. Priority ordering after reconciliation

### P0: Q3 real identity / relationship / temporal falsification

This comes first because it has no real Candidate 01 implementation evidence and directly tests the architecture-family boundary. The experiment should use real ADS semantic cases such as supersession, durable workstream identity and source/carrier change, while deliberately including cases that could force richer object machinery.

The test must ask:

```text
Can natural source-owned declarations handle the real cases cleanly?
Which semantic units genuinely need durable identity?
Which temporal/supersession facts remain source-local?
Do merge/split/move/tombstone semantics require proliferating transition objects?
Does any real case justify reopening H3/Object-Primary?
```

### P0: Q7 real public/private boundary

After Q3, qualify actual layered continuity behavior without leaking private values. Synthetic evidence is insufficient for final selection.

### P0 before selection: Q9 migration/rollback shadow

A candidate cannot be selected if it cannot be introduced and reversed without losing semantic identity, provenance or authority. This is a migration shadow, not an authority switch.

### P0 governing program: Q10

Execute final multidimensional qualification budgets and evidence accounting rather than relying on an aggregate score.

### Integrated P1 follow-ups

```text
Q1 + Q2 + Q5
    cold continuation + consequential authority task + real capture/promotion

Q4
    realistic nested workstream multi-dependency / interruption / stale-write stress
```

Q6 and Q8 remain integrated confirmation targets rather than immediate standalone research programs.

## 6. Alternative-family reopening remains live

Nothing in the recent successful real shadows makes Candidate 01 inevitable. Reopen H3/Object-Primary or another family if Q3 or later migration evidence shows that:

```text
selective source profiles require proliferating identity/transition/relation objects
AND
those objects behave like a general object substrate or central registry in practice
AND
an object-primary design materially simplifies the hard cases without unacceptable capture/migration/inspectability cost
```

Likewise, repeated special-case compatibility logic or broad hand-authored translation tables should count against Candidate 01 rather than being treated as migration trivia.

## 7. Selection disposition

Formal selection remains blocked. The original matrix still has ten formally selection-blocking clusters because no final integrated cluster pass has been recorded. Evidence triage, however, distinguishes the strongest subsystem clusters from the current dominant uncertainties:

```text
strongest real subsystem evidence
    Q6 derived views
    Q8 scaling / maintenance

current priority blocker clusters
    Q1 Q2 Q3 Q4 Q5 Q7 Q9 Q10

immediate next falsification
    Q3 real identity / relationship / temporal semantics
```

```text
RESEARCH159=WHOLE_ARCHITECTURE_EVIDENCE_RECONCILED
TOTAL_KA_ITEMS=67
ITEMS_WITH_REAL_EVIDENCE=32
ITEMS_WITH_SYNTHETIC_OR_BETTER=50
FINAL_QUALIFIED_PASSES=0
LEGACY_FILE_ORDER=REJECTED_AS_NEXT_METHOD
Q3_REAL_IDENTITY_TEMPORAL_FALSIFICATION=NEXT
TARGET_ARCHITECTURE=NOT_SELECTED
```
