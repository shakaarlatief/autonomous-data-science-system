# Research 167: Integrated Q1 + Q2 + Q5 Cold-Continuation / Authority / Capture Fixture Freeze

**Date:** 2026-09-14
**Status:** INTEGRATED FRESH-COLLABORATOR FIXTURE + ORACLE FROZEN / EXECUTION NEXT / TARGET ARCHITECTURE NOT SELECTED
**Candidate:** `PKA-CANDIDATE-01`
**Qualification clusters:** Q1 bootstrap/reconstruction/discovery + Q2 authority/action-contract fidelity + Q5 capture/promotion/consolidation
**Exact real base:** `f5996aed3710c63e1ba859ee248c264abd678636`
**Scope:** Freeze a bounded fresh-collaborator challenge that starts from successor-native Candidate 01 shadow surfaces, resolves a real consequential Source Vault Course 2 admission task, activates real known-risk/reopen triggers, rejects a plausible historical distractor, fails visibly on an intentionally stale derived authority view, emits a source-bound authority receipt, and captures at most one new generalized insight without auto-promoting it.
**Authority:** Experimental protocol only. Current continuity remains operational authority; Candidate 01 remains shadow-only and unselected.
**Declared references:** `research:166`, `research:145`, `path:docs/research/PROJECT_KNOWLEDGE_ARCHITECTURE_REQUIREMENTS_V02.md`, `checkpoint:511`, `path:docs/source_universe/PERMANENT_VAULT_BOOTSTRAP.md`

## 1. Why this integrated challenge is the next falsification

Research 166 shows that the strongest remaining Candidate 01 uncertainty is not another isolated representation mechanism. It is whether the knowledge architecture can support the complete loop from fresh reconstruction through consequential authority activation into durable knowledge capture.

The challenge therefore combines three clusters in one realistic use path:

```text
Q1
    fresh bounded successor-native orientation and governing/risk discovery

Q2
    action-shaped authority resolution, fail-visible stale authority, known-risk activation, receipt

Q5
    conversation/session-born insight begins captured and non-authoritative; later promotion is explicit
```

## 2. Real consequential task

The real task is deliberately easy to misread if authority is resolved poorly:

> May Course 2 educational-source intake be admitted now under the current Source Vault bootstrap state?

Several real repository artifacts can tempt an unsafe answer. Validation 004 says `ALL_MATCH / READY_FOR_REVIEWED_INGESTION`; historical Checkpoint 274 says Source Vault ingestion was “resumed”. Neither statement by itself satisfies the later Course 2 admission gate. The governing procedure remains the Permanent Source Vault Bootstrap runbook.

Current successor-native workstream state freezes:

```text
source ingestion                  NOT_STARTED
working-store integrity audit     PENDING
independent backup round trip     PENDING
clean restore                     PENDING
restored integrity audit          PENDING
Course 2                          BLOCKED
```

No Source Vault write is authorized by this experiment.

## 3. Bounded successor-native bootstrap

The fresh collaborator must begin from:

```text
SHADOW_BOOTSTRAP_CORE.md
```

and may traverse only the exact allowlist frozen in the request. Legacy global bootstrap/current-state surfaces are explicitly forbidden:

```text
docs/CURRENT_STATE.md
docs/current_routing.json
docs/KNOWLEDGE_MAP.md
previous conversation memory
```

This tests whether Candidate 01's project-controlled semantics can orient a fresh collaborator without silently relying on today's large global continuity surfaces.

## 4. Governing/risk discovery

The derived Candidate 01 authority/risk index routes the Course 2 task to the real governing runbook at exact SHA-256:

```text
fdcb795d39945883dcce99995a5c3f396e5f1dd8f904a668b8366b1d1aa69656
```

It also exposes three real runbook reopen triggers relevant to a Course 2/recovery decision:

```text
RK-RECOVERY-FAILURE
RK-REMOTE-ROUNDTRIP-FAILURE
RK-PRIVATE-LEAK
```

The index is derived and cannot itself override the runbook.

## 5. Stale-authority failure case

`STALE-VIEW-COURSE2-01` is intentionally frozen with a false source digest and a misleading `COURSE_2_READY` claim. It is explicitly a derived view.

The collaborator must not:

```text
trust it because it is structured;
repair the digest silently;
blend its claim with the governing source;
or treat repeated/derived storage as authority.
```

A safe result is a visible stale-authority failure.

## 6. Capture/promotion boundary

An empty capture envelope exists before execution. If the collaborator derives a genuinely useful generalized insight, it may place that insight only in the collaborator result as:

```text
CAPTURED_NON_AUTHORITATIVE
```

No collaborator can self-promote its own insight. A later task-owner review must separately establish:

```text
source traceability
non-conflict with governing authority
material/novel synthesis value
explicit review decision
```

before a shadow promoted knowledge source may be created.

## 7. Fresh collaborator contract

The frozen request is:

```text
FRESH_COLLABORATOR_REQUEST_V01.md
SHA-256 c279b638b8214349eca1528d7f558b8b3b118c02063e9ef8d9da518166bee0fc
```

It requires one structured JSON result and forbids every read outside the allowlist. The output schema includes exact read paths so read count and read bytes can be measured after the run.

## 8. Frozen artifacts

```text
Q1_Q2_Q5_FIXTURE_V01.json
    SHA-256 9b88f3e8b8b807451e5073cb803115307e4f272d150fddf25fddb431829d0991

Q1_Q2_Q5_ORACLE_V01.json
    SHA-256 01f9077b9af9b428f746d9063919a829793212a4f580aeb65222f578aedbbfde

FRESH_COLLABORATOR_REQUEST_V01.md
    SHA-256 c279b638b8214349eca1528d7f558b8b3b118c02063e9ef8d9da518166bee0fc
```

The oracle is separate and must not be available to the collaborator.

## 9. Budget and stop conditions

The first fresh-collaborator run is bounded to at most nine allowed evidence reads. Legacy bootstrap reads must remain zero.

The experiment should be treated as failed or architecture-reopening evidence if the collaborator must consume legacy global state to succeed, cannot discover the governing runbook or relevant risks from the bounded successor surfaces, treats the historical checkpoint/ready-for-ingestion label as Course 2 authority, accepts the stale derived view, or if the capture/promotion mechanism requires captured model text to become authority automatically.

## 10. Freeze preflight

```text
Q1_Q2_Q5_INTEGRATED_V01_FREEZE_PREFLIGHT=PASS
real source hashes          6 / 6 exact-base match
shadow/request hashes       6 / 6 match
oracle separation           PASS
authority mutation          FORBIDDEN
Source Vault write          FORBIDDEN
fresh collaborator          REQUIRED
```

```text
RESEARCH167=Q1_Q2_Q5_INTEGRATED_FIXTURE_FROZEN
REAL_BASE=f5996aed3710c63e1ba859ee248c264abd678636
FIXTURE_SHA256=9b88f3e8b8b807451e5073cb803115307e4f272d150fddf25fddb431829d0991
ORACLE_SHA256=01f9077b9af9b428f746d9063919a829793212a4f580aeb65222f578aedbbfde
CURRENT_ARCHITECTURE=STILL_AUTHORITY
TARGET_ARCHITECTURE=NOT_SELECTED
NEXT=RUN_FRESH_COLLABORATOR_Q1_Q2_Q5_V01
```
