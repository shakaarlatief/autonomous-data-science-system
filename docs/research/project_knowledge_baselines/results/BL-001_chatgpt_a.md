# BL-001 Operational Restart Guidance

Evidence boundary: frozen repository commit `a570f0d87b77960ae0715b291de0d5f6e884e4d0` only.

1. **Read first.** Reconstruct repository authority in the canonical order: `README.md` -> `docs/README.md` -> `docs/CONTINUITY.md` -> `docs/current_routing.json` -> `docs/CURRENT_STATE.md` -> `docs/KNOWLEDGE_MAP.md`. For the live boundary at this commit, then read Checkpoint 283 / Validation 042 / Research 117. If local execution must be restarted, use `docs/local_execution/OPERATIONS.md` together with `docs/local_execution/AUTHORITY_BOOTSTRAP.md`; do not reconstruct startup from chat memory.

2. **Prefer the bounded local runtime/tool path.** Normal operation is ChatGPT developer MCP -> OpenAI Secure MCP Tunnel -> Codexless -> Codex App Server, with Codex trust plus the Codexless workspace registry. Prefer the model-free bounded `codex.*` actions and generalized semantic `codex.git_*` contracts selected by registered `workspaceId`; do not use arbitrary shell/Git when a semantic action exists. At this frozen boundary the running accepted baseline is preview.9 / 52 tools, while preview.10 / 53 tools is only the qualified `codex.image_read` publication target.

3. **Keep the stronger fallback as emergency-only for routine repository work.** The formal model-bearing Codex-agent route is a fallback, not the normal synchronization path now that the bounded model-free semantic route is proven. Raw generic `command_exec`, arbitrary host Git, permission widening, or silent fallback to stronger authority are not routine alternatives. Host PowerShell is appropriate only for an explicitly documented host-state step that the bounded lane cannot perform, such as the currently pending guarded `codex.image_read` publication.

4. **Do not casually relaunch local startup/bootstrap helpers.** Do not start a second Codexless process when port `7690` already belongs to the intended process, and do not relaunch `codexless-http.cmd` from a fresh PowerShell without first restoring `CODEXLESS_PROFILE=ads-direct-git`, `CODEXLESS_CONFIG_OVERRIDES_FILE`, and `CODEXLESS_DEFAULT_CWD` in that same parent shell. Preserve the tunnel Git Bash shell when practical so its session-local private variables survive a controlled restart. One-time commit-zero/remote-branch bootstrap helpers are historical and no longer needed. Guarded activation helpers should be run only when the current checkpoint explicitly calls for that exact publication; at Checkpoint 283 that means one host run of `.ads-private/codexless/activate-image-read-publication.ps1 -Publish`, followed by the controlled restart sequence.

5. **Keep secrets with their real private authority.** Public Git is never secret storage, and neither private Git repository becomes a secrets manager merely because it is private. Credentials, tokens, encryption secrets, and control-plane credentials remain in secret/private operational storage. Tunnel values such as `CONTROL_PLANE_API_KEY` and `CONTROL_PLANE_TUNNEL_ID` must be rehydrated from the accepted private/local continuity source without printing them. Source Vault operational coordinates are durably owned by the private companion knowledge layer and materialized for execution in `.ads-private/source_vault_bootstrap.json`; the private local-runtime repository is only non-secret implementation/materialization evidence and does not supersede that semantic owner.

6. **Resume Research 117 next, not Source Vault ingestion.** Execute the Checkpoint 283 continuation: host-run the guarded image-read publication helper, restart/reconnect through the controlled runbook, verify preview.10 / `codexless-public-preview-v2` / 53 tools while preserving `codex.document_read`, refresh the ChatGPT developer-MCP projection if needed, then use a fresh disposable chat to call only `codex.image_read` on a known authorized PNG and require a visual-only fact for `MCP_IMAGE_TO_CHATGPT_VISION=PASS`.

7. **Why this is next.** The `codex.image_read` candidate is already regression/preflight qualified; only live host publication and the direct ChatGPT MCP-image visibility experiment remain unresolved. That experiment answers the narrow reuse-first architecture question without another Codex model turn or a custom ADS vision/rendering stack. The frozen current state explicitly keeps Source Vault ingestion paused while the broader Research 113/117 Level-2 route is active.

8. **Ignore superseded guidance.** Do not follow older Checkpoint 271-era instructions to resume Source Vault immediately; Checkpoints 276-283 supersede that transition by pausing Source Vault for the active upstream/reuse-first research. Treat historical 50/51/52-tool publication states, fixed single-repository semantic-Git assumptions, the commit-zero/private-remote bootstrap exception, and the bare `& $CodexlessLauncher` restart shorthand as historical evidence, not current operating procedure. Historical validation commands likewise do not override the evergreen runbooks, `CURRENT_STATE.md`, or the current checkpoint. Do not use descendant/current Research 124 material to reinterpret this frozen boundary.
